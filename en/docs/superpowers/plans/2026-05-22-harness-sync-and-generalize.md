# Harness Sync and Generalization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sync the latest changes from the `.claude/` harness in the zhs-web-regression-hub project to the velocitai universal template, fix remaining issues, and fill in missing permissions in settings.json.

**Architecture:** velocitai has completed most of the generalization work (27 of 28 files processed). This work focuses on 4 issues: report-strategy.md P1/P2 sections out of sync, residual class name in gen-page-test SKILL.md, and missing general permissions in settings.json. All changes are made within the velocitai repository; the zhs source project is not modified.

**Tech Stack:** Markdown、JSON（`.claude/settings.json`）

---

## File Structure

| Action | File Path | Responsibility |
|------|---------|------|
| Modify | `.claude/rules/report-strategy/report-strategy.md` | Sync P1 auto-rotation + add P2 run logging (generalized version) |
| Modify | `.claude/skills/gen-page-test/SKILL.md` | Fix residual `TestStudentHome` and duplicate path on Step 4 line 74 |
| Modify | `.claude/settings.json` | Fill in missing general permission entries |

---

### Task 1: Sync report-strategy.md P1 Auto-Rotation Section

**Files:**
- Modify: `.claude/rules/report-strategy/report-strategy.md:1-5` (frontmatter paths)
- Modify: `.claude/rules/report-strategy/report-strategy.md:83-89` (P1 section)

- [ ] **Step 1: Update frontmatter, add `utils/log_config.py` path declaration**

Replace lines 1-5:

```yaml
---
paths:
  - "conftest.py"
  - "utils/report_generator.py"
---
```

Replace with:

```yaml
---
paths:
  - "conftest.py"
  - "utils/report_generator.py"
  - "utils/log_config.py"
---
```

- [ ] **Step 2: Upgrade P1 section from "manual cleanup" to "auto-rotation"**

Replace lines 83-89 (P1 section):

```markdown
## 🟡 P1 · Report File Cleanup

**Trigger**: Temporary report files generated during verification testing or debugging.

**Rule**: Report files generated by manual verification (intentionally failing tests) must be cleaned up after verification is complete and must not be committed. The `reports/` directory is already in `.gitignore`, but excessive local accumulation still takes up disk space.
```

Replace with:

```markdown
## 🟡 P1 · Report File Auto-Rotation

**Trigger**: `utils/report_generator.py` executes cleanup logic after generating a report.

**Rule**: Keep at most **10** `report_*.html` files under `reports/html/`; when exceeded, delete the oldest reports sorted by filename.

**Implementation location**: An independent cleanup method in `utils/report_generator.py`.

```python
def _cleanup_old_reports(output_dir: Path, max_count: int = 10):
    reports = sorted(output_dir.glob("report_*.html"))
    while len(reports) > max_count:
        reports.pop(0).unlink()
```

**Call timing**: Call after `generate_html_report()` successfully writes the new report, decoupled from report generation (cleanup failure doesn't affect report writing).

❌ Anti-pattern: Clean up before report generation (may delete unread reports)

✅ Best practice: Generate new report first → then clean up reports that exceed the limit
```

- [ ] **Step 3: Add P2 run logging section after P1**

Insert between the P1 section and the "Extension Points" section:

```markdown
---

## 🟡 P2 · Run Log Recording

**Trigger**: Each time a pytest session starts.

**Rule**: Each run generates an independent log file `logs/test_run_<timestamp>.log`, and maintains a `logs/latest.log` symlink pointing to the latest log. Keep at most **100** log files.

**Implementation location**: The `setup_logging()` function in `utils/log_config.py`.

```python
def setup_logging(log_dir: str = "logs", max_files: int = 100) -> str:
    log_path = Path(log_dir)
    log_path.mkdir(exist_ok=True)

    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    log_file = log_path / f"test_run_{timestamp}.log"

    # Configure logging handler
    handler = logging.FileHandler(log_file, encoding="utf-8")
    handler.setFormatter(logging.Formatter(
        "%(asctime)s [%(levelname)s] %(name)s: %(message)s"
    ))
    logging.root.addHandler(handler)

    # Symlink
    latest = log_path / "latest.log"
    if latest.exists() or latest.is_symlink():
        latest.unlink()
    latest.symlink_to(log_file.name)

    # Rotation
    logs = sorted(log_path.glob("test_run_*.log"))
    while len(logs) > max_files:
        logs.pop(0).unlink()

    return str(log_file)
```

**Call timing**: Call in `conftest.py`'s `pytest_configure` hook.

```python
def pytest_configure(config):
    from utils.log_config import setup_logging
    setup_logging()
```

❌ Anti-pattern: Call in `pytest_sessionstart` (too late; fixture-level logs are lost)

✅ Best practice: Call in `pytest_configure` (earliest available pytest hook)
```

- [ ] **Step 4: Verify file format is correct**

Run: `head -120 /Applications/AgenticAI_2603/github/velocitai/.claude/rules/report-strategy/report-strategy.md`
Expected: frontmatter contains 3 paths, P0/P1/P2 sections complete, extension points section retained.

- [ ] **Step 5: Commit**

```bash
git add .claude/rules/report-strategy/report-strategy.md
git commit -m "sync: sync report-strategy P1 auto-rotation + P2 run logging section"
```

---

### Task 2: Fix Residual Issues in gen-page-test SKILL.md

**Files:**
- Modify: `.claude/skills/gen-page-test/SKILL.md:74`

- [ ] **Step 1: Fix residual class name and duplicate path on Step 4 line 74**

Replace line 74:

```markdown
Target file: corresponding role `tests/<role>/test_<role>_flow.py` (inheriting `<Role>BaseTest`) `tests/<role>/test_<role>_flow.py` `TestStudentHome` class.
```

With:

```markdown
Target file: `Test<Role>Flow` class in the corresponding role's `tests/<role>/test_<role>_flow.py` (inheriting `<Role>BaseTest`).
```

- [ ] **Step 2: Verify fix is correct**

Run: `grep -n "TestStudentHome\|TestStudent" /Applications/AgenticAI_2603/github/velocitai/.claude/skills/gen-page-test/SKILL.md`
Expected: No matches (zero output).

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/gen-page-test/SKILL.md
git commit -m "fix: remove residual TestStudentHome class name from gen-page-test"
```

---

### Task 3: Fill In Missing General Permission Entries in settings.json

**Files:**
- Modify: `.claude/settings.json:3-29` (allow list)

- [ ] **Step 1: Append general permission entries to the allow list**

After `"Bash(agent-browser install *)"`, before `]`, append the following entries:

```json
      "Bash(agent-browser --cdp * open *)",
      "Bash(agent-browser --cdp * snapshot *)",
      "Bash(agent-browser --cdp * eval *)",
      "Bash(agent-browser --cdp * click *)",
      "Bash(agent-browser --cdp * tab *)",
      "Bash(agent-browser --cdp * batch *)",
      "Bash(agent-browser snapshot *)",
      "Bash(agent-browser eval *)",
      "Bash(agent-browser click *)",
      "Bash(agent-browser tab *)",
      "Bash(agent-browser open *)",
      "Bash(agent-browser batch *)",
      "Bash([ -f *)",
      "Bash([ -d *)",
      "Skill(add-regression-point)",
      "Skill(add-regression-point:*)"
```

- [ ] **Step 2: Verify JSON format is correct**

Run: `python3 -c "import json; json.load(open('/Applications/AgenticAI_2603/github/velocitai/.claude/settings.json'))"`
Expected: No output (parse successful).

- [ ] **Step 3: Verify new entries exist**

Run: `grep -c "agent-browser" /Applications/AgenticAI_2603/github/velocitai/.claude/settings.json`
Expected: number >= 14 (original 4 + 10 new).

- [ ] **Step 4: Commit**

```bash
git add .claude/settings.json
git commit -m "feat: fill in agent-browser and skill general permissions in settings.json"
```

---

## Self-Review Checklist

- [x] **Spec coverage:** All 3 identified issues (report-strategy sync lag x2, gen-page-test residual, settings.json permissions missing) have corresponding Tasks
- [x] **Placeholder scan:** All code blocks and commands in Tasks have complete content, no TBD/TODO
- [x] **Type consistency:** File paths and method names are consistent across Tasks (`_cleanup_old_reports`, `setup_logging`, `Test<Role>Flow`)
- [x] **Generalization verification:** All new content contains no project-specific identifiers (no project names, specific URLs, or local paths)
